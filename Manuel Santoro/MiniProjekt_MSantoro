# Datei MiniProjket_Msantoro 
FROM ubuntu:20.04 
LABEL Manuel Santoro "manuel.santoro@edu.gbssg.ch" 
RUN     
    apt-get update && \ 
    apt-get install -y apache2 && \
    COPY C:\DockerFileHTML  /var/www/html && \
    apt-get clean && \ 
    rm -rf /var/lib/apt/lists/* 

CMD ["/bin/bash"]
